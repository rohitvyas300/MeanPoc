doctype html
head
  title Index Page
  meta(charset='utf-8')
  meta(name='viewport' content='width=device-width, initial-scale=1')
  style.
    * {
    box-sizing: border-box;
    }
    body {
    font-family: Arial, Helvetica, sans-serif;
    }
    /* Style the header */
    header {
    background-color: #666;
    padding: 2px;
    text-align: center;
    font-size: 10px;
    color: white;
    }
    /* Style the list inside the menu */
    nav ul {
    list-style-type: none;
    padding: 0;
    }
    a {
    line-height: 1em;
    display: inline-block;
    text-decoration: none;
    padding: 10px;
    margin: 10px;
    }
    article {
    float: left;
    padding: 20px;
    width: 70%;
    background-color: #f1f1f1;
    height: 300px; /* only for demonstration, should be removed */
    }
    /* Clear floats after the columns */
    section:after {
    content: "";
    display: table;
    clear: both;
    }
    table {
    font-family: arial, sans-serif;
    border-collapse: collapse;
    width: 100%;
    }
    td, th {
    border: 1px solid #dddddd;
    text-align: left;
    padding: 8px;
    }
    tr:nth-child(even) {
    background-color: #dddddd;
    }
    /* Style the footer */
    footer {
    background-color: #777;
    padding: 5px;
    text-align: center;
    color: white;
    }
    
    /* Responsive layout - makes the two columns/boxes stack on top of each other instead of next to each other, on small screens */
    @media (max-width: 600px) {
    nav, article {
    width: 100%;
    height: auto;
    }
    }
header
  h2 Certificate Portal
.cities
a(href='monthlypage') month
a(href='addaccount') Add Employee
body
    <button id="cmd">generate PDF</button>
     main#main
    //h1.ui.center.aligned.header Staff Directory
    .ui.container
      table.ui.celled.table.center.aligned
        thead
          tr
            th EmpNum
            th EmpName
            th AssignAccount
            th AgilePlanDate
            th EmpRoleCode
            th City
            th Location
        tbody
          each book in indexpage
            tr
              td #{book.EmpNum}
              td #{book.EmpName}
              td #{book.AssignAccount}
              td #{book.AgilePlanDate}
              td #{book.EmpRoleCode}
              td #{book.City}
              td #{book.Location}

script. 
  var doc = new jsPDF();
  var specialElementHandlers = {
  '#editor': function (element, renderer) {
  return true;
  }
  };
  $('#cmd').click(function () {
  console.log('clicked in ');    
  doc.fromHTML($('#content').html(), 15, 15, {
  'width': 170,
  'elementHandlers': specialElementHandlers
  });
  doc.save('C:\Users\agamj\Downloads\sample-file.pdf');
  });

footer
  p Footer
